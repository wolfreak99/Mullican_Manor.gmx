///num_replace(num, subnum, newnum)
/***************************************************
  USEAGE:
    Same as string_replace, only built for numbers.
***************************************************/

return string_replace(string(argument0), string(argument1), string(argument2));
