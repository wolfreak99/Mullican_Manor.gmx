///num_replace_all(num, subnum, newnum)
/***************************************************
  USEAGE:
    Same as string_replace_all, only formatted for numbers.
***************************************************/

return string_replace_all(string(argument0), string(argument1), string(argument2));
