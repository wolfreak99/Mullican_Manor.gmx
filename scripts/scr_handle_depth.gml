///scr_handle_depth()
/***************************************************
  USEAGE:
    Helps with the depth in the game. The further up the object is,
    the further behind layers it is.
 **************************************************/
depth = (y * -1);
