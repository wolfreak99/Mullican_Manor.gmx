///sine_get(offset, amplitude)
/***************************************************
  USEAGE:
    Creates a sine value of the sine increment and a custom offset.
    It then converts -1...1 to amp-amp.
***************************************************/

return sin(g_sine_pos + argument0) * argument1;
