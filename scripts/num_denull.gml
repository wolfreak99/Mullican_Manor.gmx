///num_denull(num, replace_what, replace_with)
/***************************************************
  USEAGE:
    Returns replace_with if num matches replace_what,
    otherwise returns num.
***************************************************/

if (argument0 == argument1)
    return argument2;
else
    return argument0;
